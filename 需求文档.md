# Upkeep 需求文档

> 文档版本：0.2  
> 产品阶段：初始需求 / MVP 规划  
> 目标平台：macOS 26.0 及以上  
> 更新日期：2026-08-21

## 1. 产品概述

Upkeep 是一款面向 macOS 的应用更新管理工具。它集中展示用户已经安装的应用，区分“有可用更新”和“无需更新”的应用，并提供搜索、版本信息查看及后续的更新操作入口。

产品采用双栏布局：左侧用于浏览和搜索应用，右侧用于展示当前所选应用的版本与发行说明等详细内容。

## 2. 产品目标

- 让用户快速了解哪些应用存在可用更新。
- 在一个界面中查看已安装应用及其当前版本状态。
- 通过应用名称快速搜索目标应用。
- 清晰展示当前版本、可用版本和发行说明。
- 使用 macOS 26 原生设计语言，保持轻量、清晰和符合系统习惯的体验。

## 3. 技术与平台约束

- 仅支持 macOS 26.0 及以上系统，无需兼容更早版本。
- UI 使用最新 SwiftUI API 开发。
- 应用源码不直接导入 AppKit；窗口生命周期和网页 URL 操作使用 SwiftUI，本地应用通过 Foundation 调用系统启动服务，图标使用 Quick Look Thumbnailing 与 Core Graphics。
- 工程、源码、资源与测试统一使用 Swift Package Manager 管理。
- 原生 Mac App Store 更新复用系统 App Store 当前登录账号，不要求用户在 Upkeep 内再次登录，也不依赖外部 `mas` 命令。
- App Store 直接更新使用系统私有框架，因此仅面向本地或 Developer ID 分发，不以提交 Mac App Store 为目标。
- 优先使用 macOS 原生窗口、侧边栏、列表、搜索、工具栏、材质及系统动画。
- 支持系统浅色与深色外观。
- 应用名称为 **Upkeep**。

## 4. 信息架构

主窗口由两个区域构成：

1. 左侧应用列表区域
2. 右侧应用详情区域

窗口应允许用户调整左右区域的宽度；左侧保持适合浏览列表的最小宽度，右侧作为主要内容区域自适应扩展。

## 5. 左侧应用列表

### 5.1 搜索

- 搜索框位于左侧边栏顶部。
- 支持按应用名称进行即时过滤。
- 搜索范围同时覆盖“可用更新”和“无需更新”两个分组。
- 搜索结果继续按照更新状态分组展示。
- 无匹配项时显示明确的空结果提示。

### 5.2 应用分组

应用列表至少分为两个区段：

- **可用更新**：存在比本机安装版本更新的版本。
- **已是最新**：当前不需要更新的已安装应用。

每个区段标题后显示应用数量。没有内容的区段默认不显示，或以简洁的空状态呈现。

### 5.3 应用列表项

每个应用列表项应包含：

- 应用图标
- 应用名称
- 本机已安装版本
- 最新版本（仅在存在更新时显示）
- 最近检查到更新的时间或日期（数据可用时显示）

列表项支持选中状态。选中后，右侧区域同步展示对应应用的详情。

### 5.4 默认选择

- 启动后优先选中“可用更新”分组中的第一个应用。
- 如果没有可更新应用，则选中“已是最新”分组中的第一个应用。
- 如果没有扫描到任何应用，则右侧显示整体空状态。

## 6. 右侧应用详情

### 6.1 顶部摘要

顶部展示当前所选应用的关键信息：

- 应用图标
- 应用名称
- 当前安装版本
- 最新版本（存在更新时）
- 更新发现日期或版本发布日期（数据可用时）

### 6.2 主要操作

- 应用存在更新时显示“更新”按钮。
- 应用无需更新时显示“打开”按钮。
- 更新进行中时展示进度或明确的处理中状态，并避免重复触发。
- 更新完成后，应用应从“可用更新”移动到“已是最新”分组，详情同步刷新。

> 注：实际更新下载、安装及权限处理方式尚未确定，首个开发阶段可先完成界面、状态流转和模拟数据，再接入真实更新能力。

### 6.3 发行说明

- 在详情区域展示目标版本的发行说明。
- 保留换行和基础文本层次，确保长内容可以滚动阅读。
- 如果应用没有提供发行说明，显示“此应用没有提供发行说明”等空状态文案。

## 7. 全局操作

参考界面中包含以下全局入口：

- **检查更新**：重新扫描或刷新全部应用的版本状态。
- **全部更新**：对所有存在可用更新的应用执行批量更新。

检查或更新期间需要有可感知的状态反馈。批量更新失败时，应能指出失败的应用，同时保留其他应用的处理结果。

## 8. 页面状态

界面需要覆盖以下状态：

- 初次扫描中
- 正常展示
- 正在检查更新
- 单个应用正在更新
- 多个应用正在批量更新
- 没有可用更新
- 没有扫描到应用
- 搜索无结果
- 无发行说明
- 获取版本信息失败
- 下载或安装失败

错误信息应简洁说明发生了什么，并在适用时提供“重试”操作。

## 9. 视觉与交互要求

- 整体遵循 macOS 26 原生视觉语言。
- 采用清晰的双栏层级，避免在右侧堆叠不必要的卡片和装饰。
- 使用系统字体、系统颜色与语义化间距。
- 选中态、悬停态、焦点态和禁用态应清楚可辨。
- 支持键盘上下键切换应用，并保持标准 macOS 焦点行为。
- 列表较长时保持流畅滚动。
- 窗口缩放后，文字不得明显截断或相互重叠。

## 10. MVP 范围

首个可运行版本包含：

- macOS 26 原生双栏主界面
- “可用更新”和“已是最新”两个应用分组
- 应用列表与选中交互
- 应用名称搜索
- 应用详情摘要
- 发行说明及其空状态
- 检查更新、更新、打开和全部更新的交互入口
- 加载、空内容、更新中、成功和失败等基础状态
- 扫描 `/Applications` 与 `~/Applications` 中的本机应用
- 读取应用名称、图标、Bundle ID、当前版本和构建号
- 首版支持原生 Mac App Store、安装在 Mac 上的 iPhone/iPad App Store 应用、Homebrew Cask、Sparkle、Electron-builder 与 Tauri updater
- 无法识别更新来源时，展示本机信息并标记为“由应用自身管理”
- 用于 SwiftUI 预览与异常状态验证的模拟数据

首版以可靠地识别更新和展示详情为优先目标。更新操作必须沿用应用的实际安装来源，不跨渠道替换应用。

## 11. 暂不纳入 MVP

- 应用商店或新应用发现
- 评分、评论和排行榜
- 应用卸载与磁盘清理
- 用户账号与跨设备同步
- macOS 26 以前系统的兼容适配
- 自动抓取任意开发者网页
- 直接安装来源不明或无法验证签名的更新包
- Microsoft AutoUpdate、Adobe Creative Cloud、Setapp、JetBrains Toolbox 等专用更新生态的深度集成
- MacPorts、Nix 与其他包管理器集成

## 12. 验收标准

- 应用可在 macOS 26.0 及以上系统启动和运行。
- 主窗口正确显示左侧应用列表与右侧详情。
- 左侧能明确区分需要更新和无需更新的应用。
- 搜索可以即时过滤两个分组中的应用。
- 选择任意应用后，右侧信息与所选应用保持一致。
- 可更新应用显示当前版本、最新版本和“更新”操作。
- 无需更新的应用显示当前版本和“打开”操作。
- 有发行说明时正常展示；无发行说明时显示空状态。
- 刷新、单个更新和全部更新入口均具备明确的过程反馈。
- 浅色、深色模式以及不同合理窗口尺寸下布局均可正常使用。
- 原生 Mac、iPhone、iPad App Store 应用能被识别并以不同平台图标标注；Homebrew Cask、Sparkle、Electron-builder 与 Tauri updater 也能被分别识别。
- 无法获得发行说明时不影响版本状态展示，也不使用第三方下载站补全信息。
- 版本或来源不确定时不会误报为可更新。
- 用户可以在“可用更新”列表右键忽略某个应用；该应用移入“已安装的应用”，并可通过右键菜单取消忽略。
- 忽略状态以 Bundle ID 持久化，重新启动或重新检查更新后仍然生效；被忽略的 Homebrew 应用不参与“更新全部”。

## 13. 首版更新来源策略

### 13.1 本机应用扫描

- 默认扫描 `/Applications` 与 `~/Applications`。
- 从应用 Bundle 读取名称、Bundle ID、`CFBundleShortVersionString`、`CFBundleVersion` 和图标。
- 应用名称优先读取与系统首选语言匹配的 `InfoPlist.strings`，空名称依次回退到 Bundle 名称和应用包文件名。
- “可用更新”和“已安装的应用”均按完整时间戳（精确到秒）降序排列：已安装应用使用应用包及其 Info.plist、可执行文件中最新的本地修改时间；可用更新优先使用发布日期，没有发布日期时回退到该修改时间。界面日期只显示到日。没有时间或时间相同时使用名称和路径保证顺序稳定。
- 重新检查期间保留当前列表、滚动位置和选择；扫描及来源检查完成后一次性替换为最终结果，避免中间状态重组列表导致跳动。
- 系统应用与 Upkeep 自身默认不参与第三方更新检查。
- 当前版本以应用 Bundle 中的数据为准，不从网页反推本机版本。

### 13.2 来源识别顺序

按实际安装来源优先匹配：

1. 检测原生 Mac App Store receipt；对于 iPhone/iPad 应用，检测 `Wrapper/iTunesMetadata.plist` 和包装应用信息。
2. 检测 Sparkle.framework 及可读取的 `SUFeedURL`。
3. 检测 Electron-builder 的 `app-update.yml` / `app-update.yaml`；仅接受 `provider: github`（含 owner/repo）或带 HTTPS 地址的 `provider: generic`。
4. 检测 Tauri updater 的 HTTPS `latest.json` / `update-proxy.json` 端点。
5. 将应用路径与本机已安装的 Homebrew Cask 对应；不覆盖 App Store、Electron-builder 或 Tauri。
6. 仍无法识别时标记为“由应用自身管理”。

同一应用存在多个公开下载渠道时，不以“哪个版本号更高”决定来源，避免跨渠道覆盖、许可证变化或签名冲突。

### 13.3 App Store

- 通过 App Store receipt 的存在性识别可能来自 Mac App Store 的应用，再结合 Bundle ID 查询商店信息；首版不承担 receipt 的密码学验签。
- 使用应用 Bundle ID 和当前商店地区查询公开的 App Store Lookup 数据，并明确请求 `desktopSoftware`，不使用可能混入 iPhone/iPad 版本的通用软件结果。
- 查询结果必须包含 `MacDesktop-MacDesktop` 设备标记；Bundle ID 相同但仅支持 iPhone/iPad 的记录不得用于版本比较。
- 对安装在 Apple 芯片 Mac 上的 iPhone/iPad 应用，读取 `Wrapper/iTunesMetadata.plist` 中的商店 ID 和地区，并根据 `UIDeviceFamily` 识别 iPhone 或 iPad 平台。
- iPhone/iPad 应用使用商店 ID 查询对应的移动平台记录，不使用 `desktopSoftware` 结果；查询结果仍须与本地 Bundle ID 和设备平台匹配。
- 应用列表中的 App Store 来源统一使用苹果 Logo，保持快速识别；详情页来源徽标在苹果 Logo 后附加较小的 `macwindow`、`iphone`、`ipad` 区分平台，标题显示为“Mac App Store”“iPhone App Store”或“iPad App Store”。
- 可用时展示最新版本、发布日期、发行说明、图标和商店页面。
- App Store 数据不可用、应用下架或地区不可用时，只保留本机信息，不推测更新状态。
- “更新”操作交给 Mac App Store 完成，Upkeep 不自行替换 App Store 应用。

### 13.4 Homebrew Cask

- 仅在本机已安装 Homebrew 时启用此来源。
- 使用 Homebrew 的 JSON 输出读取已安装 Cask、当前版本和可用版本。
- 使用 Homebrew 自身的版本判断结果，不自行解释 `latest` 等特殊版本。
- Homebrew 通常不提供发行说明；可展示来源、主页及版本，发行说明允许为空。
- 普通 Cask 可通过 Homebrew 执行更新；`brew outdated` 仍报告过期的 `auto_updates` Cask 同样提供「更新」，由 Upkeep 执行 `brew upgrade --cask --greedy-auto-updates`。
- 未过期且声明为 `auto_updates` 的 Cask 仍交给应用自身更新。
- 已识别为 Electron-builder 或 Tauri updater 的应用不再改标为 Homebrew，以便沿用该副本实际使用的更新协议。

### 13.5 Sparkle

- 检测应用内置 Sparkle.framework 和静态配置的 `SUFeedURL`。
- 解析 Appcast 中与当前 macOS 版本和处理器架构兼容的更新项。
- 可用时展示版本、构建号、发布日期和发行说明。
- 动态生成、需要鉴权或无法访问的 Feed 不强行解析，应用标记为自身管理。
- Appcast 提供 HTTPS 安装包时，使用 Upkeep 内嵌的 Sparkle 2 官方流程进行下载、签名验证、原子安装及必要的应用重启。
- Appcast 仅提供版本说明、没有安全下载项或无法由 Sparkle 安装时，操作入口仍打开应用并由其自身更新器完成。

### 13.6 Electron-builder

- 读取 `Contents/Resources/app-update.yml` 或 `app-update.yaml`，按通用协议识别，不按应用名适配。
- `provider: github` 需要明确的 owner 与 repo，检查 `releases/latest/download/latest-mac.yml`，必要时回退到 GitHub Releases API 寻找同名清单。
- `provider: generic` 需要 HTTPS `url`，检查该地址下的 `latest-mac.yml`。
- 不处理 `custom`、`keygen`、空地址、localhost、HTTP 或声明为 `private` 的源。
- 解析清单中的版本、发布日期、发行说明，并按当前架构优先选择 zip，其次 dmg。
- 仅下载 HTTPS 安装包；清单提供 sha512 时必须校验通过。安装前核对 Bundle ID，若两端都能读到 Team ID 则还须一致。

### 13.7 Tauri updater

- 优先读取 `tauri.conf.json` 中的 updater endpoints；否则在主可执行文件中提取 HTTPS 的 `latest.json` 或 `update-proxy.json`。
- 含 Electron Framework 的应用不按 Tauri 扫描，避免误识别。
- 解析 JSON 中的 `version`、`notes`、`pub_date` 和 `platforms`，优先选择 `darwin-aarch64` / `darwin-x86_64` 的 `.app` 压缩包。
- 仅下载 HTTPS 安装包，安装前核对 Bundle ID 与可选的 Team ID。GitHub 只可能作为该 JSON 或安装包的托管地址，不再单独作为更新来源。

### 13.8 信息降级规则

详情按照以下层级呈现：

1. 有最新版本和发行说明：完整展示。
2. 有最新版本但无发行说明：展示版本，并显示“此版本未提供发行说明”。
3. 只能识别管理来源：展示来源和本机版本，提供“打开应用”。
4. 无法识别来源：展示本机版本并标记“由应用自身管理”。
5. 数据不确定或请求失败：不判定为可更新，提供重试入口。

不通过搜索引擎、聚合下载站或非官方镜像补充版本与下载地址。

### 13.9 更新与安全边界

- Upkeep 仅调用已确认的安装来源进行更新。
- 不把版本字符串当作小数比较；优先采用来源自身的更新判断与版本顺序。
- 执行更新前显示目标版本和更新来源。
- 应用正在运行、来源不可用或需要额外权限时给出明确提示。
- 首版的“全部更新”只处理能够安全自动更新的来源：Homebrew Cask、带安装包的 Sparkle / Electron-builder / Tauri，以及可用的 Mac App Store 应用。
- Electron-builder 与 Tauri 的直接安装必须通过 HTTPS，并验证 Bundle ID；有 sha512 时校验哈希，两端都能读到 Team ID 时还须一致。

### 13.10 分发方式

Upkeep 计划采用 Developer ID 签名与 Apple 公证后在 Mac App Store 之外分发。原因是扫描其他应用并调用 Homebrew 等外部程序与 Mac App Store 强制 App Sandbox 的限制不兼容。

开发构建阶段不要求正式签名，但工程结构需保留 Hardened Runtime 和后续公证能力。

## 14. 后续待确认事项

以下内容不阻塞首版开发，后续根据实际使用结果决定：

1. 是否允许用户添加自定义应用扫描目录。
2. 是否自动定时检查更新，以及默认检查频率。
3. 是否允许忽略某个版本或将某个应用排除在检查范围之外。
4. 是否接入 Microsoft AutoUpdate 等厂商专用更新器。
5. 是否提供需要管理员权限的辅助安装工具。
